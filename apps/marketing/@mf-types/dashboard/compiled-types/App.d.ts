import { History, MemoryHistory } from 'history';
type Props = {
    history: MemoryHistory | History;
};
export default function App({ history }: Props): import("react/jsx-runtime").JSX.Element;
export {};
